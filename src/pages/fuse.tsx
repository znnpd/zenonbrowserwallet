import * as React from "react";
import {Layout} from "../components/layout";
import {Box} from "@mui/material";

export const FusePage = () => {
    return (
        <Layout title="Fuse QSR">
            <Box/>
        </Layout>
    )
}
